mutation AddChildMetadata($id: String!, $metadataId: String!) {
    content {
        collection {
            addChildMetadata(id: $id, metadataId: $metadataId) {
                id
            }
        }
    }
}
