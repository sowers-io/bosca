query GetMetadataSupplementaryJson($id: String!, $key: String!) {
    content {
        metadata(id: $id) {
            supplementary(key: $key) {
                content {
                    json
                }
            }
        }
    }
}