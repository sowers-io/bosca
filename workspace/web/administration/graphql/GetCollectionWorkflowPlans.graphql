query GetCollectionWorkflowPlans($id: String) {
    content {
        collection(id: $id) {
            workflow {
                plans {
                    ...WorkflowPlan
                }
            }
        }
    }
}