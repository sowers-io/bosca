fragment CollectionTemplate on CollectionTemplate {
    configuration
    defaultAttributes
    collectionFilter {
        options {
            ...FindQueryOption
        }
    }
    attributes {
        key
        name
        description
        type
        supplementaryKey
        ui
        list
        configuration
        workflows {
            workflow {
                ...Workflow
            }
            autoRun
        }
    }
}

