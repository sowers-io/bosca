fragment WorkflowPlan on WorkflowExecutionPlan {
    id {
        ...WorkflowExecutionId
    }
    complete
    active
    failed
    error
    cancelled
    workflow {
        ...PlanWorkflow
    }
}