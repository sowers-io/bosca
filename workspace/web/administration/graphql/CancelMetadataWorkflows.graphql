mutation CancelMetadataWorkflows($id: String!, $version: Int!, $workflowId: String!) {
    workflows {
        cancelWorkflows(metadataId: $id, metadataVersion: $version, workflowId: $workflowId)
    }
}