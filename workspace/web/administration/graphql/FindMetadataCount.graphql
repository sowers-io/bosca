query FindMetadataCount($attributes: [FindAttributeInput!]!, $extension: ExtensionFilterType, $categoryIds: [String!], $contentTypes: [String!]) {
    content {
        findMetadataCount(attributes: $attributes, extensionFilter: $extension, categoryIds: $categoryIds, contentTypes: $contentTypes)
    }
}
