mutation BeginMetadataTransition($id: String!, $version: Int!, $state: String!, $status: String!) {
    workflows {
        beginTransition(request: {metadataId: $id, version: $version, stateId: $state, status: $status, waitForCompletion: true})
    }
}