mutation BeginMetadataTransition($id: String!, $version: Int!, $state: String!) {
    workflows {
        beginTransition(request: {metadataId: $id, version: $version, stateId: $state, status: "User Update", waitForCompletion: true})
    }
}