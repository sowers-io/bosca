mutation AddStorageSystem($system: StorageSystemInput!) {
    workflows {
        storageSystems {
            add(storageSystem: $system) {
                ...StorageSystem
            }
        }
    }
}