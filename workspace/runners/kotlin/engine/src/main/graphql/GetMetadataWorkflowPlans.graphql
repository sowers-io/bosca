query GetMetadataWorkflowPlans($id: String!) {
    content {
        metadata(id: $id) {
            workflow {
                plans {
                    ...WorkflowPlan
                }
            }
        }
    }
}