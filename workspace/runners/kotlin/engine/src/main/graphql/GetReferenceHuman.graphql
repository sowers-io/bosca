query GetBibleReference($id: String!, $version: Int, $usfm: String!) {
    content {
        metadata(id: $id, version: $version) {
            bible {
                chapter(usfm: $usfm) {
                    reference {
                        human
                    }
                }
            }
        }
    }
}
