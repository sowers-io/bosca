mutation SetCollectionSystemAttributes($id: String!, $attributes: JSON!) {
    content {
        collection {
            setCollectionSystemAttributes(id: $id, attributes: $attributes)
        }
    }
}