mutation DeleteSupplementaryMetadata($planId: String, $id: String!, $key: String!) {
    content {
        metadata {
            deleteSupplementary(planId: $planId, id: $id, key: $key)
        }
    }
}