mutation SetCollectionLocked($id: String!, $locked: Boolean!) {
    content {
        collection {
            setLocked(id: $id, locked: $locked) {
              ...Collection
            }
        }
    }
}
