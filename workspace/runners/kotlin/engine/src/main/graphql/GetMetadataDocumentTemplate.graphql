query GetMetadataDocumentTemplate($id: String!, $version: Int) {
    content {
        metadata(id: $id, version: $version) {
            documentTemplate {
                ...DocumentTemplate
            }
        }
    }
}
