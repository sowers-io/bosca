mutation EditStorageSystem($id: String!, $input: StorageSystemInput!) {
    workflows {
        storageSystems {
            edit(id: $id, storageSystem: $input) {
                ...StorageSystem
            }
        }
    }
}