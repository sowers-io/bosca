query GetCollectionRelationships($id: String!) {
    content {
        collection(id: $id) {
            metadataRelationships {
                ...CollectionRelationship
            }
        }
    }
}