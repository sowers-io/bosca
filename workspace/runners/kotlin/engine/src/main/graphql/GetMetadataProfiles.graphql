query GetMetadataProfiles($id: String!, $version: Int) {
    content {
        metadata(id: $id, version: $version) {
            profiles {
                ...MetadataProfile
            }
        }
    }
}