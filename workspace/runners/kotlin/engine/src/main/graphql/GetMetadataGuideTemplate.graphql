query GetMetadataGuideTemplate($id: String!, $version: Int!) {
    content {
        metadata(id: $id, version: $version) {
            guideTemplate {
                ...GuideTemplate
            }
        }
    }
}
