mutation MergeCollectionItemAttributes($id: String!, $itemId: String!, $attributes: JSON!) {
    content {
        collection {
            mergeCollectionItemAttributes(id: $id, itemId: $itemId, attributes: $attributes)
        }
    }
}