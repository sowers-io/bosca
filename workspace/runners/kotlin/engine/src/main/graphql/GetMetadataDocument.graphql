query GetMetadataDocument($id: String!, $version: Int!) {
    content {
        metadata(id: $id, version: $version) {
            document {
                ...Document
            }
        }
    }
}
