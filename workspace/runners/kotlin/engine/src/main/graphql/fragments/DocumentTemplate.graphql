fragment DocumentTemplate on DocumentTemplate {
    metadata {
        id
        version
        slug
    }
    configuration
    schema
    content
    attributes {
        key
        name
        description
        type
        ui
        workflows {
            workflow {
                ...Workflow
            }
            autoRun
        }
        configuration
    }
}

