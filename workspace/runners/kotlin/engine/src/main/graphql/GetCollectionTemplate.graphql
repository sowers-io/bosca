query GetCollectionTemplate($id: String!, $version: Int) {
    content {
        metadata(id: $id, version: $version) {
            collectionTemplate {
                ...CollectionTemplate
            }
        }
    }
}
