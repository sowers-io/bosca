query GetMetadataCategories($id: String!, $version: Int) {
    content {
        metadata(id: $id, version: $version) {
            categories {
                ...Category
            }
        }
    }
}