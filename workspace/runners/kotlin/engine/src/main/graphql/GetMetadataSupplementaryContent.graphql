query GetMetadataSupplementaryContent($supplementaryId: String!) {
    content {
        metadataSupplementary(supplementaryId: $supplementaryId) {
            content {
                ...MetadataSupplementaryContent
            }
        }
    }
}