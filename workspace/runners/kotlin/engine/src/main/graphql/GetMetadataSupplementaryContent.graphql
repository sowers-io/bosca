query GetMetadataSupplementaryContent($id: String!, $key: String!) {
    content {
        metadata(id: $id) {
            supplementary(key: $key) {
                ...MetadataSupplementary
            }
        }
    }
}